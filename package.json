{
  "name": "corsproxy-extension",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run build.ts --watch",
    "build": "bun run build.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/chrome": "^0.0.280",
    "bun-types": "^1.3.5",
    "typescript": "^5.7.2"
  }
}
